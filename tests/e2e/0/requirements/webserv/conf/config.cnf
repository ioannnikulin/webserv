server {
    listen 172.18.0.10:8080;
    server_name localhost;

    location / {
        root /var/www/html;
        index index.html;
    }

    location /folder {
        root /var/www/html/folder;
        index index.html;
        listable true;
    }
}