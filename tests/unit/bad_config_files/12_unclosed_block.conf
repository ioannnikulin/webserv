server {
    listen 127.1.0.1:8080;
    server_name localhost;

    location / {
        root tests/unit/volume;
        index index.html;

    location /upload {
        root tests/unit/volume/uploads;
        methods POST;
        upload on tests/unit/volume/uploads/tmp;
    }
}