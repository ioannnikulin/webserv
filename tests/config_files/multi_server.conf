server {
    listen 8080;
    server_name example.com;

    location / {
        root /var/www/example;
        index index.html;
    }
}

server {
    listen 9090;
    server_name api.localhost;

    location /api {
        root /var/www/api;
        index index.json;
    }
}