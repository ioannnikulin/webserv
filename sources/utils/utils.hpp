#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

#include "colors.hpp"

void printSeparator(void);
void setColor(std::string color);
void resetColor(void);
void waitForInput();
void clearTerminal();

#endif
